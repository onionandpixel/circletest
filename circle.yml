machine:
    environment:

dependencies:
    override:
        - chmod +x gradlew
        - ./gradlew dependencies 

test:
    override:
        - ./gradlew clean assembleDebug -PdisablePreDex
        - cp -r circletest/build/outputs $CIRCLE_ARTIFACTS
